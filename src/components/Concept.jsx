import React from "react";

const Concept = ({ data }) => {
  return (
    <div className="">
      <div className="">
        <h1>{data.title}</h1>
      </div>
    </div>
  );
};

export default Concept;
